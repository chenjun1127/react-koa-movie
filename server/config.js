const config = {
    // 数据库配置
    database: {
        DATABASE: 'node_sql',
        USERNAME: 'root',
        PASSWORD: 'cj123456',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;